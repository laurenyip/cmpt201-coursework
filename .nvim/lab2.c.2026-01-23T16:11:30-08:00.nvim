#include <stdio.h>
#include <sys/types.h>
#include <sys/wait.h>
#include <unistd.h>


int main() {
  while(true) {
    


    if ((len = getline(&line, &len, stdin)) != -1) {
      line[len -1] = '\0';

      pid_t pid = fork();
      if (pid != 0){

        pid_t wpid = waitpid(pid, NULL, 0);
        if(wpid==-1){
          printf("ERROR waiting for PID\n");
        }

      } else {
        if (execl(line, line, NULL) == -1){printf("Exec failure\n");
        }
      }
    }else{

